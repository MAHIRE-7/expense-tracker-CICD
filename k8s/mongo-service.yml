apiVersion: v1
kind: Service
metadata: 
  name: mongo-service
  namespace: et-ns
spec:
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
  selector:
    app: mongo
  type: ClusterIP